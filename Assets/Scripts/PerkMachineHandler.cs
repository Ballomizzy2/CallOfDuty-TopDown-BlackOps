using UnityEngine;

public class PerkMachineHandler : MonoBehaviour
{
    //player interact event
    //[SerializeField] private 
    //when player raycasts, it checks if player has. if yes, gui n can buy. else no gui and no buy
    //holds a list of all perks
    //holds a list of current perks w/ a max of 4
}
